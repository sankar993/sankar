#include <stdio.h>
int main() {
    int i,n=5,c=0;
 for(i=1;i<=n;i++){
        if(n%i==0)
        c=c+1;
    }
 if(c==2)
        printf("the number is prime");
    else 
    printf("the number is composite");
    return 0;
}
